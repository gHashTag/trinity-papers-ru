# arXiv:2606.05017 — English upload package (v3)

**Paper 1 — GoldenFloat.** This file is the English metadata + change-log for the
next arXiv replacement. The compiled PDF is `main_ru.pdf` (Russian body + English
abstract page at the end; the English abstract below is byte-identical to that
page). Paste the *Title* and *Abstract* fields into the arXiv "replace" form;
attach the recompiled `main_ru.pdf` (or the `.tex` source).

> Honesty tags used throughout (kept out of the abstract, present in the body):
> `[proven]` / `[measured]` / `[measured — GPU]` / `[measured — SW proxy]` /
> `[measured CI-synth]` / `[verified SW on iverilog]` / `[modeled]` /
> `[open conjecture]` / `[routing-pending]` / `[requires user action]`.

---

## Title

GoldenFloat: a family of static-split floating-point formats derived from the
golden ratio φ, from GF4 to GF256, with an exact integer Lucas identity

## Authors

D. V. Vasilev — Independent researcher, Ko Samui, Thailand — admin@t27.ai —
ORCID 0009-0008-4294-6159

## Abstract (arXiv abstract field)

We present a hardware-oriented description of GoldenFloat (GF) — a family of
static-split floating-point formats generated by a single closed-form rule —
together with three concrete artefacts: (i) an open multi-width RTL generator
covering GF4–GF256 with a continuous-integration differential sweep against a
correctly-rounded reference; (ii) a Lucas-exact integer accumulator path
verified to 500 digits for n = 1,…,256; and (iii) a GF16 FPGA codec passing a
35/35 testbench at 323 MHz on Artix-7 (Xilinx XC7A100T). For every full width
N ≥ 4 the exponent-field width is e = round((N−1)/φ²) with fraction width
f = N−1−e and φ = (1+√5)/2. The rule reproduces the realised exponent widths of
nine formats (9/9) and extends consistently to further widths. It is positioned
alongside posit (Posit Standard 2022), takum (Hunhold 2024, 2025), OCP-MX
(Rouhani et al. 2023) and the IEEE P3109 multi-width floating-point draft. The
breadth/toolchain-coherence thesis is registered as an open conjecture with a
pre-registered falsification path (ledger FL-002); we make no per-step accuracy
or superiority claim. An RTL correctness erratum (2026-05-31) is reported: the
fabricated TTSKY26b dies carry a defective multiplier portfolio, and the
corrected generator is the baseline for regeneration.

## Keywords

numeric formats; floating point; golden ratio; Lucas numbers; posit; takum;
OCP-MX; arithmetic hardware.

---

## Change-log v2 → v3 (honest edits only)

All edits keep the binding honesty rules (no "first/only/best/beats"; separate
encoding ≠ compute ≠ FPGA; catalog = 83; Tier-E = full 4/4 chain only).

1. **Hardware Tier-E status refreshed to the live #199 count (2026-07-13).**
   decode-HW 47 → **53** formats; on-silicon union of both axes 49 → **55**
   unique formats; compute-HW stays **10** GF (gf4/6/8/10/12/14/16/20/24/32),
   both-axes overlap stays **8** (gf4/6/8/10/12/14/16/20). gf24/gf32 decode is
   `[routing-pending]` (horizon B), so they are not "both axes" even with the
   new compute burst.

2. **Compute is ADD and MUL only — NOT ADD+SUB+MUL.** No separate subtraction
   RTL exists in the repository; the reference harness
   `conformance/gf16_compute_conformance_ax7203.py` takes `--op {add,mul}`. The
   earlier "ADD+SUB+MUL" wording was an over-count and is corrected.

3. **IDCODE typo fixed:** expected board IDCODE `0x13631093` → **`0x13636093`**
   (ALINX AX7203, XC7A200T-2FBG484I).

4. **Lucas identity — measured refutation added (honest reframe).** A
   `[measured — SW proxy]` result refutes any φ-specific accumulation benefit:
   GF16 (6e/9m) accumulates bit-for-bit as an ordinary float of the same fields
   (harness `lucas_accum_test.py`, seed 20260706); an fp32 accumulator remains
   mandatory for all 16-bit formats including GF16. This *strengthens* the
   thesis "φ is a field-selection rule, NOT different arithmetic"; the exact
   Lucas identity φ²ⁿ+φ⁻²ⁿ=L₂ₙ remains a purely algebraic witness (unchanged).

5. **Next contribution (in progress) added to the conclusion.** GF+A — a
   per-row adaptive container (per-row scale + per-row pocket selection from a
   φ-catalog). Framed strictly: GF+A is `≥ every tested fixed format` by the
   *selection metric* (MSE/SQNR) on the measured data; the honesty boundary is
   stated explicitly — the guarantee is on the selection metric, NOT downstream:
   a direct model bits-per-token measurement shows ΔBPT two orders of magnitude
   below the significance threshold. No "beats a class" claim.

6. **Bibliography — two 2026 prior-art entries added and cited** in the
   related-work section (§6):
   - **Tekum** — L. Hunhold, "Tekum: balanced ternary tapered precision real
     arithmetic," 2025, arXiv:2512.10964. Same author as takum; tapered on
     balanced ternary. Niches explicitly separated (Tekum = tapered on ternary;
     GF = φ static split on binary); no superiority claimed, cited as an
     adjacent line of work.
   - **b-posit** — A. A. Jonnalagadda, R. Thotli, J. L. Gustafson, "Closing the
     gap between float and posit hardware efficiency," 2026, arXiv:2603.01615.
     Constrains the posit regime field to ≤6 bits, −60…79 % decoder cost — an
     independent confirmation that fixed fields cheapen hardware.

Both new arXiv references were verified live (fetch of the arXiv abstract pages).
